# ==========================================
# AURORA Auth Service - Environment Example
# ==========================================

# Core connectivity
DATABASE_URL="postgresql://user:password@postgres-db:5432/database?schema=public"
FIREFLY_API_URL=http://localhost:5000/api/v1/namespaces/default

# SMTP / Mail delivery
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM=noreply@aurora.es
PASSWORD_RESET_ACTION_URL=http://localhost/reset

# JWT (RS256)
# Provide PEM keys directly or base64-encoded PEM in one line.
# NEVER commit real private keys.
JWT_PRIVATE_KEY=<BASE64_OR_PEM_PRIVATE_KEY>
JWT_PUBLIC_KEY=<BASE64_OR_PEM_PUBLIC_KEY>
JWT_ACCESS_EXPIRES_IN=5m
JWT_REFRESH_EXPIRES_IN=24h

# Refresh cookie policy (used by auth controller)
# Development defaults:
REFRESH_COOKIE_SECURE=false
REFRESH_COOKIE_SAMESITE=lax
# Production recommended values:
# REFRESH_COOKIE_SECURE=true
# REFRESH_COOKIE_SAMESITE=strict

# Redis (ephemeral blacklist / distributed security cache)
REDIS_HOST=redis
REDIS_PORT=6379

# Runtime / test helpers
NODE_ENV=development
TEST_OWNER_ID=1
SEQ_URL=http://seq:5341
SEQ_API_KEY_AUTH=
SEQ_FIRSTRUN_ADMINUSERNAME=
SEQ_FIRSTRUN_ADMINPASSWORD=
# If auth runs outside Docker Compose, use:
# SEQ_URL=http://localhost:5341

# HIBP password breach check (k-anonymity API)
HIBP_PASSWORD_CHECK_ENABLED=true
HIBP_PWNED_PASSWORDS_BASE_URL=https://api.pwnedpasswords.com/range
HIBP_TIMEOUT_MS=5000
# false => fail-open if HIBP is unavailable, true => fail-closed
HIBP_FAIL_CLOSED=false
# Optional: required only for authenticated HIBP endpoints
HIBP_API_KEY=
HIBP_USER_AGENT=AURORA-Auth-Service
